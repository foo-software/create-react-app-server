{
  "name": "root",
  "private": true,
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "packages/**/src/**/*.{js,jsx,ts,tsx,json,css,scss,md}": [
      "prettier --single-quote --write",
      "git add"
    ]
  },
  "devDependencies": {
    "husky": "^3.0.1",
    "lerna": "^3.2.1",
    "lint-staged": "^9.2.0",
    "prettier": "^1.18.2"
  }
}
